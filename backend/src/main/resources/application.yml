spring:
  application:
    name: fashion-fit-ai-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres.zitdvycxzromyzsghbne}
    password: ${DB_PASSWORD:Av1234##Udi12}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080

fashionfitai:
  jwt:
    secret: ${JWT_SECRET:change-this-development-secret-to-a-long-random-string-before-production}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

logging:
  level:
    com.fashionfitai: DEBUG