# --- Backend (Maven / Java) ---
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
!.mvn/wrapper/maven-wrapper.properties
HELP.md

# Local-only Spring profile — holds real DB credentials (e.g. Supabase password).
# Never commit this; application.yml falls back to local Postgres without it.
backend/src/main/resources/application-local.yml

# --- Frontend (Node / Vite) ---
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Environment files ---
.env
.env.local
.env.*.local

# --- IDE ---
.idea/
*.iml
.vscode/
!.vscode/extensions.json

# --- OS ---
.DS_Store
Thumbs.db

# --- Logs ---
*.log
